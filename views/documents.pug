extends layout.pug

block content
    .container.full-page
        .centered-content(style='width: 90%')
            h2 All documents
            each report in reports 
                a(href=`/documents/${report.reportId}`) #{report.reportId}
            
    
